# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env files (.env.local holds user-specific secrets and overrides)
.env
.env.local

# Editor/IDE
# .idea/
# .vscode/

# Local build artifacts
/bin/
# Release artifacts built by `make dist`; published to GitHub Releases,
# never committed.
/dist/
/dist/
/tmp/

# Stray `go build` artifacts: running `go build ./examples/...` from
# the repo root drops the resulting binary at the root, named after
# the example's directory. Match the existing examples explicitly so
# we don't accidentally ignore real source folders that share the
# layout (rare, but possible).
/01-end-to-end
/02-cli-end-to-end
/03-deploy-end-to-end
/04-router-in-path

# Local observability data (mounted into Grafana stack containers)
/deploy/data/

# Local-only docker-compose overrides
docker-compose.override.yaml
deploy/docker-compose.override.yaml

# Session handoff notes: local working state, not project docs.
HANDOFF_*.md

# Raw provider capacity samples (hack/capacity-sample.sh). The log grows
# per run; a distilled artifact gets committed, the raw series does not.
capacity-samples.jsonl

# Paid measurement-run artifacts (hack/measure-run.sh). The distilled
# csv is what gets committed, not the raw run directory.
measure-runs/

# Built calculator site; regenerate with 'make export-pages' in docs/calculators
docs/calculators/output/

# Per-machine Claude Code settings
.claude/settings.local.json
